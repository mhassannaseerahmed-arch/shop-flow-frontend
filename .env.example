# API URL - Set this to your deployed backend URL
REACT_APP_API_URL=https://your-backend.vercel.app/api

# Disable ESLint plugin during build (prevents warnings from failing the build)
DISABLE_ESLINT_PLUGIN=true
